gcp:
  type: object
  properties:
    options:
      type: object
      properties:
        GCP_FOO:
          type: string
          export_env: GCP_FOO
          required: true
          default: bar