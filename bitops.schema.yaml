gcp:
  type: object
  properties:
    options:
      type: object
      properties:
        GCP_FOO:
          type: string
          export_env: GCP_FOO
          required: true
          default: bar
        GCP_BAR:
          type: string
          export_env: GCP_BAR
          required: true
          default: rab
